// CommonJS, every file is module (by default)
// Modules - Encapsulated Code (only share minimum)

//getting all the name from the one module file
// const names=require('./4-name');


// const sayHi=require('./5-utils');

//getting the name directly from the module
// const {mihir,deep}=require('./4-name');
// console.log(names);


//accees the name by using key property
// sayHi('susan');
// sayHi(names.mihir);
// sayHi(names.deep);


//acccess the name directly 
// sayHi('susan');
// sayHi(mihir);
// sayHi(deep);


//objects and array values getting
// const data=require('./6-alternative-flavour');
// console.log(data);

//bunch of whole stuff operation using single file
require('./7-mind-grenade');










